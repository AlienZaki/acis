<?xml version="1.0" encoding="utf-8"?>
<!-- Debug only: allow cleartext for LAN brain connections (ws://).
     Release builds have usesCleartextTraffic=false in the manifest. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
